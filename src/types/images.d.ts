/**
 * Типы для статических импортов изображений (`import hero from "@/images/hero.webp"`).
 *
 * Их же объявляет `next-env.d.ts`, но он генерируется сборкой и лежит в
 * .gitignore: в CI `typecheck` идёт до `build`, файла ещё нет, и все импорты
 * снимков падают с TS2307. Коммитить его нельзя — он тянет `./.next/types`,
 * которого до сборки тоже не существует. Поэтому ссылка на типы своя.
 */
/// <reference types="next/image-types/global" />
